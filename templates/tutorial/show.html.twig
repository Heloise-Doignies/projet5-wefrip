{% extends 'base.html.twig' %}
{% block title %}{{ tutorial.tutoName }}{% endblock %}

{% block body %}
<div class="container w-75">

 
<a href="{{ path('app_tutorial') }}" class="btn btn-primary mb-3"><i data-feather="arrow-left"></i> Retour</a>

 <div class="row w-100 d-flex justify-content-center">

	<div class="col-12">
        <div class="card">
                <video controls id="video">
                <source src="{{ asset('img/videoTuto/' ~ tutorial.tutoVideoName) }}" type="video/mp4"></source>
                </video>

        <div class="d-flex justify-content-around">
            <div class="card-body">
                <h5 class="card-title">{{ tutorial.tutoName }}</h5>
                <p class="card-text">{{ tutorial.tutoDescription }}</p>
            </div>

            <div class="d-flex flex-column align-items-end">
               <i data-feather="heart" class="text-danger mb-3 m-2"></i>
                <a href="#" class="btn btn-primary mb-3 m-2">Catégorie</a>
                <a href="#" class="btn btn-warning mb-3 m-2">Support</a>
            </div>

        </div>
        </div>
    </div>
</div>

</div>
{% endblock %}